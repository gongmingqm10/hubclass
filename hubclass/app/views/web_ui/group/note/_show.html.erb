<h2 class="text-center">{{note.title}}</h2>
<div>
  <p class="text-center text-info"> {{note.author.username}}  发表于{{note.updated_at | date: 'yyyy-MM-dd'}}</p>
  <span data-ng-repeat="tag in note.tags" class="icon icon-tags text-primary">{{tag}}</span>
</div>
<hr>
<div gm-dynamic-compile="note.content"></div>
<div data-ng-show="note.attachments">
<hr>
<p> 附件 </p>
<a data-ng-repeat="attachment in note.attachments" data-ng-href="{{attachment.url}}"><span class="icon icon-attach"></span>{{attachment.filename}}</a>
</div>
<br>