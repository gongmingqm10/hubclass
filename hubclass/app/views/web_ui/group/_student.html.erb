<div class="table-responsive">
  <table class="table table-hover">
    <thead>
    <tr>
      <th>姓名</th>
      <th>学号</th>
      <th>班级</th>
      <th>赞</th>
    </tr>
    </thead>
    <tbody>
    <tr data-ng-repeat="group in groups">
      <td>{{group.course.name}}</td>
      <td>{{group.name}}</td>
      <td>{{group.teacher.username}}</td>
      <td><a data-ng-href="/course/{{group.id}}">进入</a></td>
    </tr>
    </tbody>
  </table>
</div>