<div class="table-responsive">
  <table class="table table-hover">
    <thead>
    <tr>
      <th>课程</th>
      <th>课堂</th>
      <th>任课教师</th>
      <th>操作</th>
    </tr>
    </thead>
    <tbody>
    <tr data-ng-repeat="group in groups">
      <td>{{group.course.name}}</td>
      <td>{{group.name}}</td>
      <td>{{group.teacher.username}}</td>
      <td><a href="javascript:void(0);" data-ng-click="enter_group(group.id)">进入</a></td>
    </tr>
    </tbody>
  </table>
</div>